package com.insurance.claims.model;

/**
 * Enum representing user roles in the system
 */
public enum UserRole {
    USER,    // Regular user who can submit claims
    ADMIN    // Admin who can review and process claims
}

